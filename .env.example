# Optional: receive contact form submissions directly.
# Create a free form at https://formspree.io and paste its endpoint here,
# then rebuild. Without this the form opens the visitor's mail client instead.
VITE_CONTACT_ENDPOINT=
